import styled from 'styled-components';

const HeaderComponent = styled.header``;

const Header = () => {
  return (
    <HeaderComponent>
      <h1>헤더</h1>
    </HeaderComponent>
  );
};

export default Header;
